{
  "permissions": {
    "defaultMode": "auto"
  },
  "model": "opus[1m]",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills/learned/_gen-index.sh\"",
            "timeout": 10,
            "statusMessage": "Loading learned-skills index"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "/root/miniconda3/bin/cs",
    "refreshInterval": 1
  },
  "enabledPlugins": {
    "context7@claude-plugins-official": true,
    "frontend-design@claude-plugins-official": true
  },
  "language": "Russian",
  "tui": "fullscreen",
  "skipWorkflowUsageWarning": true,
  "theme": "dark-daltonized",
  "switchModelsOnFlag": true,
  "remoteControlAtStartup": true,
  "agentPushNotifEnabled": true,
  "autoMode": {
    "soft_deny": [
      "$defaults",
      "Bash(rsync:*--delete*) targeting .82 or .81 office hosts in this project — mass-delete during sim/training relay",
      "Bash(rm:*) under conda envs / training data dirs on .81 (training server) — irreversible local destruction on a remote host"
    ],
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: private (nzahar/follow_me_camera, confirmed via gh)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured — repo uses `.env` (gitignored) with `.env.example` template; SIM_/TRAIN_ SSH keys and passwords for office hosts",
      "**Default / protected branches**: main (default); no rulesets or branch protection listed via gh",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: two office machines accessed via SSH — `.82` (sim VM, Gazebo render/inference only) and `.81` (training server, detector training/eval only), per docs/runbooks/server.md",
      "**Source control**: the trusted repo (nzahar/follow_me_camera) and its origin remote (github.com:nzahar/follow_me_camera.git) only",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: SIM_HOST and TRAIN_HOST (office machines defined in .env.example — sim VM and training server respectively; hostnames themselves not populated in the template)",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: `.env` (SSH host/user/key/password credentials for SIM_ and TRAIN_ machines) — credentials only, share with no one; keep out of commits and PRs",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace/host/container with `prod` or `production` as a whole word/segment (none observed in this repo's evidence)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development (aerial-cinematography / tail-sitter drone interception project — follow_me_camera)",
      "**Trusted repo**: /root/Projects/follow_me_camera (github.com:nzahar/follow_me_camera.git, private) — confidential material (non-secret) is fine to commit here; secrets and credentials are never cleared in by visibility",
      "**Org-specific CLIs**: None configured — project uses conda, rsync, sshpass, rclone for office-host workflows (tools/train_ssh.sh, tools/sim_known_hosts, tools/train_known_hosts helpers)",
      "**routine under follow_me_camera/ prefix**: rsync/sshpass/scp-based transfers between `.82` (sim) and `.81` (training) hosts per docs/runbooks/server.md are routine project workflow, not ad-hoc remote access"
    ]
  }
}
