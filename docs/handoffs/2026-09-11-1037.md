# Handoff — the framework writes more documentation than it can carry; decide what to delete

For a fresh session with no prior context. Read this, then the files in § Read first,
then continue from § Next steps. Reply in Russian.

## Goal

Rework `/root/.claude` toward less documentation, not better-managed documentation. This session
investigated why the codemap size cap never fired on `follow_me_camera`, proposed a fix, and the
owner rejected the fix as more machinery — «зря мы столько документируем, что тащить уже не можем
за собой груз этот. Хорошо когда нечего убрать, а не когда нечего добавить». The measurements
below are the durable output; the deletions in § Next steps are proposals, not decisions.

**Work only inside `/root/.claude`. Do not open, read or reason about the `follow_me_camera`
project** (owner's instruction, 2026-09-11). Every number this rework needs is already in this
file; that project's own cleanup is a separate task.

## Current state

Nothing is in flight. `main` at `f29e13a`, no branch open, no plan file. The only dirty path is
untracked `/root/.claude/commands/` — six `statusbar-*.md` stubs dated 2026-09-01, unrelated to
this session and untouched by it.

The plan for the rejected design and its branch were both deleted this session
(`/root/.claude/docs/plans/size-cap-measures-bytes.md`, never committed;
`fix/size-cap-measures-bytes`). Do not resurrect either — the numbers that mattered are here.

## Done this session

Measured all 55 codemaps under `/root/Projects/*/docs/CODEMAPS/*.md`, then the plan, ADR and
handoff corpora across six projects; figures in § Verification status. Traced the size cap's
failure to its causes in `/root/.claude/lib/doc-compaction-contract.md` and
`/root/.claude/rules/workflow.md`. Wrote and deleted the fix plan after the owner rejected it.

## What did NOT work

**The proposed fix to the size cap was rejected, and the rejection is the finding.** The design was:
measure bytes instead of lines, measure the protected meaning-layer block too, calibrate thresholds
against the 55-file corpus. It was correct on its own terms and still wrong — a bloat meter that
fires accurately does not reduce what gets written. Owner's verdict: «Какие-то танцы бесполезные.»
Do not re-propose a threshold change in any unit.

No tooling failed this session. No commands errored.

## Key decisions

- **Delete the size-cap mechanism rather than repair it.** Read this one in full before touching
  step 1 of § Next steps — it is the reasoning the owner asked to have written down, not a summary.

  What the mechanism is: rules in `/root/.claude/lib/doc-compaction-contract.md` that watch a
  codemap's size and force `document-agent` to shorten the file once it crosses a threshold.

  It does not work. Across the 55 codemaps in `/root/Projects/*/docs/CODEMAPS/` it flags one file,
  because it counts **lines** while these files hold single lines up to 54 578 characters.

  Repairing the measurement was this session's proposal and the owner rejected it. The rejection is
  correct, and the reason is the part worth keeping: **an accurate size meter still does not reduce
  what gets written.** It only shortens what already exists — and the way it shortens is to move
  surplus prose out of a structural table and into the meaning-layer block, which the same contract
  declares uncounted (`lib/doc-compaction-contract.md:22` «never counted and never compacted», the
  relocation at `:41` and `rules/workflow.md:111`). So the measured size falls while the file does
  not. That is not compaction, it is relocation into a blind spot. The file where that ran to
  completion is `/root/Projects/follow_me_camera/docs/CODEMAPS/control.md` — the smallest
  structural portion in the whole corpus, 50 lines, inside a 244 KB file. For `REPORT.md` the same
  relocation lands in a counted section, so the blind spot is codemap-only.

  What it costs: roughly a hundred framework lines — the 65-line contract, D8 in
  `/root/.claude/rules/workflow.md`, and wiring in `document-agent`, `experiment-doc-agent` and
  `code-reviewer`. A hundred lines for one flagged file.

  Hence the proposal is not a better threshold in a better unit. It is deleting all hundred lines,
  and letting "write less" replace the mechanism outright. Not yet approved.
- **One branch gets documented four times** — plan, ADR, codemap `## Data flow` bullet,
  `docs/history.md` entry — and the codemap bullet is the derived copy, 276 KB of `sim-infra.md`'s
  639 KB. Step 2 of § Next steps acts on this. **Unverified as a general claim**: two of 71 bullets
  were sampled.
- **Two operational lessons from that handoff need one line each, not a mechanism**: do not edit
  files a dispatched agent is writing (it stashed and restored itself mid-run), and do not poll a
  background run with `pgrep -f <pattern>` (it matches the wrapper shell running that very command).

## Next steps

Proposals for the owner to accept or reject, ordered by how much they remove.

1. **Cut the size cap.** Delete `/root/.claude/lib/doc-compaction-contract.md`, D8 from
   `/root/.claude/rules/workflow.md`, and the D8 wiring at `agents/code-reviewer.md:95`,
   `agents/experiment-doc-agent.md:55,166`, `agents/document-agent.md:48,80`. Keep the four
   pass-cost levers (`read-once`, `read-parallel`, `batch-edit`, `scope-strict`) somewhere — they
   are the only part with observable effect.
2. **Decide which artifact owns a branch's story, and make the other three pointers.** Candidate:
   ADR owns *why*, codemap owns *current structure only* with no chronological narrative, plan and
   history.md are deleted at merge. This is the change that stops the growth; everything else
   trims symptoms.
3. **Shrink or retire the plan artifact.** `follow_me_camera` holds 92 plans averaging 28 KB —
   2.8 MB against 1016 KB of codemaps. Nothing ever retires one. Either plans are deleted at merge
   (git keeps them) or the full-track template shrinks hard.
4. **Re-examine the four document agents.** `document-agent`, `experiment-doc-agent`,
   `plan-reviewer` and `handoff-reviewer` are 888 of the framework's 2021 lines. `plan-reviewer` is
   the single largest file at 280 lines and reviews a document read once.
5. **Stop archiving handoffs.** The framework calls a handoff ephemeral, then `pickup-handoff`
   moves it to `docs/handoffs/archive/` permanently — 78 files, 786 KB fleet-wide. Git already
   remembers.
6. **Add the two one-line rules** from § Key decisions to `CLAUDE.md` §Sub-agents and
   §Tool Hygiene, if and only if the rework keeps those sections.

## Gotchas

- `/root/Projects/follow_me_camera/docs/CODEMAPS/*.md` contains single lines up to 41 330
  characters, and `/root/Projects/big_dataset_handling/docs/CODEMAPS/training-harness.md` one of
  54 578. Any line-oriented tooling over these files gives meaningless answers. Measure bytes.
- Reading those codemaps costs real context: the `follow_me_camera` set alone is 1.04 MB. Measure
  with a shell pass; do not Read them to find out how big they are.
- Use `/root/miniconda3/bin/python` for any measurement script; the dev box has no project env.
- Cutting `follow_me_camera`'s own codemaps is that project's task, not the framework's, and this
  session's rework must not enter that repo at all — see the instruction under § Goal.

## Verification status

- Codemap corpus → a Python pass over `/root/Projects/*/docs/CODEMAPS/*.md` run this session:
  55 files; structural portion median 18 KB, p90 65 KB, max 159 KB; protected block median 21 KB,
  p90 117 KB, max 530 KB.
- Cap fires on 1 of 55 → same pass, counting files at or above 400 structural lines: only
  `/root/Projects/big_dataset_handling/docs/CODEMAPS/phase-b.md` (443).
- `sim-infra.md` = 639 KB total, 530 KB protected, 254 structural lines; `control.md` = 244 KB
  total, 220 KB protected, 50 structural lines → same pass.
- Framework size → `wc -l` over `CLAUDE.md rules/*.md lib/*.md agents/*.md` = 2021 lines; the four
  document agents plus the contract = 888.
- Corpus counts → `ls | wc -l` per project: 233 plans, 126 ADRs, 55 codemaps, 78 handoffs.
  `follow_me_camera`: 92 plans, 2648 KB, average 28 KB.
- **UNVERIFIED — that the codemap `## Data flow` section is genuinely a duplicate** of the plan,
  ADR and history entries. Two bullets were sampled; 71 exist. The claim originates in
  `/root/Projects/follow_me_camera/docs/handoffs/archive/2026-09-11-1009.md`, which also marks it
  unverified.
- **UNVERIFIED — that deleting the size cap costs nothing.** No project was checked for a codemap
  whose growth the cap actually restrained.
- **UNVERIFIED — a 182 MB `docs` figure** measured for `big_dataset_handling`. Dominated by
  non-prose files under `docs/`, never broken down. Do not quote it.

## Git snapshot

```
$ git branch --show-current
main

$ git status --porcelain
?? commands/

$ git log --oneline -5
f29e13a fix(rules): trim plain-language bullets to what a model can check (#49)
27b2599 feat(rules): write session replies for a reader outside the session (#48)
c658610 feat(workflow): retire delegation-by-default, drop slice-implementer (#47)
9766a01 feat(workflow): one-round plan review, drop cap/loop machinery, demote size metrics (#46)
108dd76 feat(roadmap): event-triggered ## Now lifecycle - clear at merge-readiness, ask when empty (#45)

$ git diff --name-only
(empty)
```

## Read first

1. `/root/.claude/lib/doc-compaction-contract.md` — 65 lines, the mechanism proposed for deletion.
   Its line 32 predicted its own failure mode and deferred the fix.
2. `/root/.claude/rules/workflow.md` § Documentation economy — 44 lines, D1–D9, the rule set that
   governs document size. Step 1 of § Next steps removes D8 from it.
3. `/root/.claude/agents/plan-reviewer.md` — 280 lines, the framework's largest file, for step 4.

Deliberately not on this list: the `follow_me_camera` handoff that reported the symptoms. Its
content is summarised in § Key decisions and § Verification status, and the owner asked that this
rework stay out of that project.
